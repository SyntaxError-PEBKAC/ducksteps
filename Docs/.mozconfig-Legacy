ac_add_options --enable-application=browser
ac_add_options --enable-release
ac_add_options --disable-debug
ac_add_options --enable-unified-build
ac_add_options --enable-optimize
ac_add_options --enable-lto=full
ac_add_options --disable-artifact-builds
ac_add_options MOZ_PGO=1
mk_add_options MOZ_MAKE_FLAGS=-j32

export CC=clang-cl
export CXX=clang-cl
export LINKER=lld-link
export HOST_LINKER=lld-link

# Skylake Tuning
export CFLAGS="-march=skylake -mtune=skylake"
export CXXFLAGS="-march=skylake -mtune=skylake"

mk_add_options MOZ_OBJDIR=D:/ducksteps-obj/esr140-Legacy   # sibling, same structure

ac_add_options --with-branding=browser/branding/ducksteps