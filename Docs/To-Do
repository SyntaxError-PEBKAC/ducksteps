Coming Soon™️:

Need to update some branding images like installer embedded images.

Simultaneously release another version of ducksteps where the zen5 flags are replaced to optimize for Intel Core i5-8365U era PCs (AVX2 support).

Create custom PGO python scripts to simulate human scrolling movement and custom website testing.
